/**
 * CGo OpenMap - 福州车站卡片模块 (city/fuzhou/stacard/script.js)
 *
 * 由编辑器生成的占位实现：车站信息板中暂不渲染周边地图卡片。
 * 若需接入高德切片周边地图，请参照 city/qingdao/stacard/script.js 实现
 * window.福州StaCard 的 init / hasCard / getCardPlaceholderHtml / renderPanelCards 方法。
 */

const FuzhouStaCard = {
    async init() { return false; },
    hasCard() { return false; },
    getCardPlaceholderHtml() { return ""; },
    async renderPanelCards() { return false; }
};

window.FuzhouStaCard = FuzhouStaCard;
